# Protocol

Solidity contracts, Circom circuits, and Hardhat tests for Aegis on Sonic.

```bash
npm ci
npm run compile
npm test
```

Architecture diagrams: [`architecture/`](architecture/).

Contract addresses (mainnet): [`../docs/network.md`](../docs/network.md).

Formal verification (Certora, TGE + oracle scope) is published here only after all scoped rules pass on Certora Prover.
