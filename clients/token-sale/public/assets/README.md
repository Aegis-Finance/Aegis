# `public/assets/`

No logo images are bundled here. Optional `logoURI` values in `public/tokenlists/aegis.tokenlist.json` should point to immutable URLs you host (e.g. Arweave) if wallets need icons.
